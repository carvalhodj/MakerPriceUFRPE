<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/backgroundGray"
    tools:context="makerprice.com.makerpriceufrpe.LoginActivity" >

    <ImageView
        android:id="@+id/signInLogo"
        android:src="@drawable/logo_makerprice_icon"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:layout_gravity="center_horizontal"
        android:contentDescription="@string/descricao_imagem_tela_inicial"/>

    <EditText
        android:id="@+id/sign_in_usuario"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/campoEmailHint"
        android:inputType="textEmailAddress"
        android:maxLines="1"
        android:layout_marginTop="16dp"
        android:layout_below="@id/signInLogo"/>

    <EditText
        android:id="@+id/sign_in_senha"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/campoSenhaHint"
        android:inputType="textPassword"
        android:maxLines="1"
        android:layout_marginTop="24dp"
        android:layout_below="@id/sign_in_usuario"/>

    <Button
        android:id="@+id/botaoSignIn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/textoBotaoSignUp"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="24dp"
        android:padding="12dp"
        android:layout_below="@id/sign_in_senha"
        android:gravity="center_horizontal"
        android:onClick="onButtonClick"/>

    <TextView
        android:id="@+id/linkCadastrar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/linkSignIn"
        android:textSize="16sp"
        android:textAlignment="center"
        android:gravity="center_horizontal"
        android:layout_below="@+id/botaoSignIn"
        android:layout_centerHorizontal="true"
        android:onClick="onButtonClick"/>

</RelativeLayout>
